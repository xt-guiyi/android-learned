<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidLearned" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@android:color/holo_orange_light</item>
    </style>

    <style name="Theme.AndroidLearned" parent="Base.Theme.AndroidLearned" />

    <style name="Theme.AndroidLearned.Button" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="materialButtonOutlinedStyle">@style/Widget.App.OutlinedButton</item>
    </style>
</resources>